import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Not Found",
  description:
    "The page you are looking for could not be found. Please check the URL or return to Cleanz Store to continue shopping.",
};

const page = () => {
  return <div>NOT FOUND PAGE</div>;
};

export default page;
